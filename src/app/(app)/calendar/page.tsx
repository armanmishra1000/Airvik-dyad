"use client";

import { AvailabilityCalendar } from "@/components/shared/availability-calendar";

/**
 * Renders the calendar page containing the availability calendar.
 *
 * @returns The page's JSX element that wraps an AvailabilityCalendar component.
 */
export default function CalendarPage() {
  return (
    <div>
      <AvailabilityCalendar />
    </div>
  );
}