import { ForgotPasswordForm } from "@/components/auth/forgot-password-form";

/**
 * Renders the forgot-password page by mounting the ForgotPasswordForm component.
 *
 * @returns The page's JSX element containing the ForgotPasswordForm.
 */
export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}