import { LoginForm } from "@/components/auth/login-form";

/**
 * Render the login page containing the login form.
 *
 * @returns The JSX element for the login page that renders the `LoginForm` component.
 */
export default function LoginPage() {
  return <LoginForm />;
}