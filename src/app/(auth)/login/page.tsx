import { LoginForm } from "@/components/auth/login-form";

/**
 * Renders the login form page.
 *
 * @returns The JSX element containing the LoginForm component.
 */
export default function LoginPage() {
  return <LoginForm />;
}